<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoobarApplication"
        tools:targetApi="31">
        <activity
            android:name=".activities.Comment_Activity"
            android:exported="false" />
        <activity
            android:name=".entities.Comment"
            android:exported="false" />
        <activity
            android:name=".adapters.CommentListAdapter"
            android:exported="false" />
        <activity
            android:name=".activities.Activity_Post"
            android:exported="true" />
        <activity
            android:name=".activities.signup_Activity"
            android:exported="false" />
        <activity
            android:name=".activities.FriendsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>